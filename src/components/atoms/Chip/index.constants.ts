export const DEFAULT_CHIP_ELEMENT = "span" satisfies React.ElementType;

export const CHIP_VARIANTS = ["fill", "outline", "tune"] as const;

export const CHIP_COLORS = ["error", "success", "warning"] as const;
